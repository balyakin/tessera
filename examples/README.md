# Tessera Examples

`semantic-demo.docx` and `semantic-demo.odt` are deterministic synthetic manuscripts generated by:

```sh
go run ./internal/demo/generate
```

They demonstrate named paragraph and character styles that survive into IR, LaTeX, and EPUB.
